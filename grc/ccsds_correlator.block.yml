# auto-generated by grc.converter

id: ccsds_correlator
label: Correlator
category: '[CCSDS]'

parameters:
-   id: asm
    label: ASM
    dtype: int
    default: '0x1acffc1d'
-   id: asm_mask
    label: ASM Mask
    dtype: int
    default: '0xffffffff'
-   id: threshold
    label: Threshold Mask
    dtype: int
    default: '0'
-   id: frame_len
    label: Frame Length
    dtype: int
    default: '223'

inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: message
    id: out

templates:
    imports: import ccsds
    make: ccsds.correlator(${asm}, ${asm_mask}, ${threshold}, ${frame_len})

file_format: 1
